#include <stdio.h>

int main (void) {
	float a, b, c, d, e, f, g;
	int h, i, j, k, l, m, n;
	
	printf ("%f %f %f %f %f %f %f\n", a, b, c, d, e, f, g);
	printf("%d %d %d %d %d %d %d\n", h, i, j, k, l, m, n);
}